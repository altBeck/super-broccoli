import { StudioPlaceholder } from "@/components/studio/StudioPlaceholder";

export default function StudioIdeasPage() {
  return (
    <StudioPlaceholder
      title="Ideas"
      description="Future idea vault for raw concepts, theses, and references."
    />
  );
}
