import { StudioPlaceholder } from "@/components/studio/StudioPlaceholder";

export default function StudioNowPage() {
  return (
    <StudioPlaceholder
      title="Now"
      description="Future editor for current status, banner text, and now page fields."
    />
  );
}
