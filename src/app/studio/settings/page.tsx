import { StudioPlaceholder } from "@/components/studio/StudioPlaceholder";

export default function StudioSettingsPage() {
  return (
    <StudioPlaceholder
      title="Settings"
      description="Future studio preferences and integration settings."
    />
  );
}
