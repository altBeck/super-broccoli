import { StudioPlaceholder } from "@/components/studio/StudioPlaceholder";

export default function StudioNotesPage() {
  return (
    <StudioPlaceholder
      title="Notes"
      description="Future notes drafting and publishing workspace."
    />
  );
}
