import { StudioPlaceholder } from "@/components/studio/StudioPlaceholder";

export default function StudioProjectsPage() {
  return (
    <StudioPlaceholder
      title="Projects"
      description="Future project tracker and case study builder entry point."
    />
  );
}
